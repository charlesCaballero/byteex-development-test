import React from "react";

const IconAvatar = ({ children }: { children: React.ReactNode }) => {
  return <div className="rounded-full bg-[#F9F0E5] p-2">{children}</div>;
};

export default IconAvatar;
